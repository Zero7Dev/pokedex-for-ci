name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Saludando
        run: |
          echo "Hola Julio!"
      - name: Dime la fecha
        run: date
      - run: ls -l